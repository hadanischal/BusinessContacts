# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BusinessContacts' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for BusinessContacts
  pod 'SwiftLint'
  
  target 'BusinessContactsTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'BusinessContactsUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
end
